# TODO - Add-to-cart TikTok flight animation

- [x] Add CSS for cart-fly clone element animation styling.
- [x] Update `index.js` to create a floating image clone on each “Add to Cart” click.
- [x] Compute start (product image) and end (cart icon) coordinates via `getBoundingClientRect()`.
- [x] Animate clone with scaling + easing + slight fade toward the cart icon.
- [x] Remove the clone after animation completes; verify no console errors.



